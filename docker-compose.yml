version: "3.7"

services:
    dev:
        container_name: home-finances
        env_file: ./.env
        build:
            context: .
            dockerfile: ./Dockerfile
        ports:
            - 4200:4200
        volumes:
            - .:/usr/src/app
            - /usr/src/app/node_modules
        restart: unless-stopped
